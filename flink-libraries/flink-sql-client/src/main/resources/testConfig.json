{
  "output": "kafka",
  "jar": "",
  "watermarkInterval": 1000,
  "defaults": {
    "kafka": {
      "output-topic-prefix": "out",
      "properties": {
        "bootstrap.servers": "localhost:9092",
        "zookeeper.connect": "localhost:2181",
        "group.id": "test"
      }
    }
  }
}
